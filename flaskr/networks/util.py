import networkx as nx 

# The Random Graphs
# gnm_random_graph(n, m, seed=None, directed=False)
def genrandomgraph()
# The 4 centralities
# degree_centrality(G):  Compute the degree centrality for nodes.
# eigenvector_centrality_numpy(G, weight=None, max_iter=50, tol=0):Compute the eigenvector centrality for the graph G.
# closeness_centrality(G, u=None, distance=None, wf_improved=True, reverse=False):Compute closeness centrality for nodes.
# betweenness_centrality(G, k=None, normalized=True, weight=None, endpoints=False, seed=None):Compute the shortest-path betweenness centrality for nodes.

# The Community-detection
# girvan_newman(G, most_valuable_edge=None): Finds communities in a graph using the Girvan–Newman method.

